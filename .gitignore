# Compiled python modules.
*.pyc

# Setuptools distribution folder.
/dist/

# Python egg metadata, regenerated from source files by setuptools.
/*.egg-info

# Data for the app (might contain secrets, so I removed it for that purpose)
rsconnect
.Rproj.user
